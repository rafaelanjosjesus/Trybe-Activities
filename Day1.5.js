let weekDay = "Wednesday"
if(weekDay === "Monday" || weekDay === "Tuesday" || weekDay === "Wednesday" || weekDay === "Thursday" || weekDay === "Friday"){
    console.log("Yay, another day of learning at Trybe >:D");
} else {
    console.log("FINALLY, well deserved rest! UwU!")
}