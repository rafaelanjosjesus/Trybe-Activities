//Percorra o array imprimindo todos os valores contidos nele com a função console.log(). -- Iterate through the array by printing all the values ​​contained in it with the console.log().

let numbers = [5, 9, 3, 19, 70, 8, 100, 2, 35, 27];
    for (let index = 0; index < numbers.length; index += 1) {
        console.log(numbers[index]);
    }